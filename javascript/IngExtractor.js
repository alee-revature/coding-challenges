function ingExtractor(word) {
    //split string into array
    word.split();

    //find whichever has -ing inflection

    //for each -ing inflection, print
    print();

    //parse everytime there's a space. use space as a delimiter
}